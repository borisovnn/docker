#какой базовый образ берем
FROM python:alpine
#путь к директории в контейнере, которую будем использовать для проекта (нельзя указывать корень, может перезаписать файлы системы)
WORKDIR /app
#скопировать файлы из текущей директории (относительный путь .) в текущую указанную workdir директорию контейнер
COPY . .
#запуск комадой python файла main.py
CMD [ "python", "main.py" ]